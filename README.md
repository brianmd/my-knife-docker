Dockerfile for an image that works with chef's knife gem.

Will need:
```sh
  $ gem install knife-docker
```

and
```sh
  $ run ./build.sh
```

You'll want to replace the authorized_keys file with your own public ssh key(s).
